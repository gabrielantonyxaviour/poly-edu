type Certificate @entity {
  id: ID!
  projectId: BigInt!
  student: Bytes!
  certificateHash: Bytes!
  issueTimestamp: BigInt!
}

type Course @entity {
  id: ID! # projectId
  creator: Bytes!
  course_meta: String!
  certificate_meta: String!
  created_at: BigInt!
  isCompleted: Boolean
}
